<template>
  <section class="risk-setting-select">
    <h2>{{ label }}</h2>
    <div class="item">
      <span
        v-for="(item, index) in list"
        :class="{ active: item.code == selectCode }"
        :key="index"
        @click="handle(item, selAttCode, selProductCode)"
        >{{ item.text }}</span
      >
    </div>
  </section>
</template>

<script>
export default {
  name: "riskSettingSelect",
  components: {},
  props: {
    //被选中的code
    selectCode: {
      type: String,
      default: function() {
        return "";
      }
    },
    //label
    label: {
      type: String,
      default: function() {
        return "";
      }
    },
    list: {
      type: Array,
      default: function() {
        return [];
      }
    },
    //附加险按钮组 当前选中attcode
    selAttCode: {
      type: String,
      default: function() {
        return "";
      }
    },
    ///附加险按钮组 当前选中附加险code
    selProductCode: {
      type: String,
      default: function() {
        return "";
      }
    }
  },
  data() {
    return {};
  },
  computed: {},
  watch: {},
  created() {},
  mounted() {},
  // beforeUpdate() {
  //   this.initComponents();
  // },
  methods: {
    //选中某项
    handle(item, selAttCode, selProductCode) {
      this.$emit("handle", item, selAttCode, selProductCode);
    }
  }
};
</script>

<style lang="scss" scoped>
.risk-setting-select {
  margin: 0 20px;
  padding: 15px 0;
  display: flex;
  justify-content: space-between;
  border-bottom: 1px solid $color-ds;
  h2 {
    font-size: 15px;
    color: #333333;
    font-weight: normal;
    max-width: 90px;
  }
  .item {
    max-width: 230px;
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;

    span {
      display: inline-block;
      min-width: 60px;
      height: 24px;
      line-height: 24px;
      text-align: center;
      border: 1px solid #e5e9f1;
      border-radius: 4px;
      font-size: 12px;
      color: #333;
    }
    span.active {
      background: $color-theme;
      border-color: $color-theme;
    }

    span:nth-child(3n-2) {
      margin: 6px;
    }
    span:nth-child(3n-1) {
      margin: 6px;
    }
    span:nth-child(3n) {
      margin: 6px;
      margin-right: 0;
    }
    span:last-child {
      margin-right: 0;
    }
    span:nth-child(1) {
      margin-top: 0;
    }
    span:nth-child(2) {
      margin-top: 0;
    }
    span:nth-child(3) {
      margin-top: 0;
    }
  }
}
</style>
